describe('test username input', function () {
    
    it('Does not do much', function() {
        cy.visit()
        cy.contains('username')
            expect(true).to.equal(true);
        })
    })