{
  "version": 2,
  "name": "user",
  "builds": [{ "src": "user/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
  "routes": [
      { "src": "/(.*)", "dest": "user/$1" }
  ]
}