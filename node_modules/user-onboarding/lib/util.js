"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/** 定时轮询 */
function makeIntervalPoll(callback, ms) {
    var timer = setInterval(callback, ms);
    return function () {
        clearInterval(timer);
    };
}
exports.makeIntervalPoll = makeIntervalPoll;
function makeEventListener(callback, event, target) {
    if (target === void 0) { target = window; }
    target.addEventListener(event, callback);
    return function () {
        target.removeEventListener(event, callback);
    };
}
exports.makeEventListener = makeEventListener;
function isEqualOrChild(child, parent) {
    for (var current = child; !!current; current = current.parentElement) {
        if (current === parent)
            return true;
    }
    return false;
}
exports.isEqualOrChild = isEqualOrChild;
function debounce(func, wait, immediate) {
    var timeout;
    return function () {
        var context = this, args = arguments;
        var later = function () {
            timeout = null;
            if (!immediate)
                func.apply(context, args);
        };
        var callNow = immediate && !timeout;
        clearTimeout(timeout);
        timeout = setTimeout(later, wait);
        if (callNow)
            func.apply(context, args);
    };
}
exports.debounce = debounce;
//# sourceMappingURL=util.js.map