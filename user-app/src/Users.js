import React from 'react'

function Users(props) {
    return (
        <div>
            {/* {props.user.map((data,i) => {
                <div key={i}>
                    <p>{data.name}</p>
                    <p>{data.email}</p>
                    <p>{data.password}</p>
                </div>
            })} */}
        </div>
    )
}

export default Users
